apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: usuarios-api-sm
  namespace: monitoring
  labels:
    release: kps
spec:
  selector:
    matchLabels:
      app: usuarios-api
  namespaceSelector:
    matchNames: [ fcg-dev ]
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scheme: http
